package BroCodeJava;

public class MathClass {
	public static void main(String[] args) {
		double x=3.14, y=-10;
		//you haven't declared the "Z" variable in order to use it....??
		
		double z = Math.max(x,y);//compares the largest of two numbers and displays that
		// double z = Math.min(x,y);//compares the smallest of two numbers and displays that
		// double z = Math.abs(x);//displays the absolute number
		// double z = Math.sqrt(y);//displays the square root
		// double z = Math.round(x);//displays the round figure 
		// double z = Math.floor(x);//displays the round down figure
		// double z = Math.ceil(x);//displays the round up figure
		
		System.out.println(z);
		
		
		
		
		
	}

}
